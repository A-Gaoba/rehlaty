# Server
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database
MONGODB_URI=mongodb+srv://gaoba:gaoba123@cluster0.nthnq.mongodb.net/arabic-tourism-social

# Auth (JWT via jose)
JWT_SECRET=gaoba1234
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Cookies
COOKIE_NAME=auth_token
REFRESH_COOKIE_NAME=refresh_token
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost

# Uploads / Images
UPLOADS_MAX_FILE_SIZE_MB=10
IMAGE_MAX_WIDTH=2000
IMAGE_MAX_HEIGHT=2000
IMAGE_QUALITY=80

# Realtime (choose one and configure)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=

ABLY_KEY=

# Misc
VERCEL_ANALYTICS_ENABLED=false

# Auth cookie name used by app
NEXTAUTH_COOKIE_NAME=app_token

